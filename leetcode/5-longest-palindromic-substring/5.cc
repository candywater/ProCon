#include <iostream>
class Solution {
public:
    string longestPalindrome(string s) {

    }

    string reverseStr(string s){
      string res(s.size());
      for(int i = 0; i < s.size(); i++){
        res
      }
    }
};
