// Generated by CoffeeScript 2.3.2
(function() {
  /*
   * Definition for a binary tree node.
   * function TreeNode(val) {
   *     this.val = val;
   *     this.left = this.right = null;
   * }
   * @param {TreeNode} root
   * @return {boolean}
   */
  var dfs, flg, isUnivalTree, num;

  flg = false;

  num = -1;

  isUnivalTree = function(root) {
    flg = true;
    num = root.val;
    dfs(root);
    return flg;
  };

  dfs = function(node) {
    if (node === null) {
      return;
    }
    console.log(node);
    if (node.val !== num) {
      flg = false;
      return;
    }
    dfs(node.right);
    dfs(node.left);
  };

}).call(this);
